# Cloudflare R2 Configuration
# Copy this file to .env.local and fill in your actual R2 credentials

AWS_ACCESS_KEY_ID=your-r2-access-key-id
AWS_SECRET_ACCESS_KEY=your-r2-secret-access-key
AWS_REGION=auto
AWS_ENDPOINT_URL=https://<your-r2-bucket-id>.r2.cloudflarestorage.com
S3_BUCKET_NAME=s3-uploader

# Development mode (allows missing credentials for testing)
NODE_ENV=development

# Instructions:
# 1. Copy this file: cp env.example .env.local
# 2. Get your R2 credentials from Cloudflare dashboard
# 3. Replace the placeholder values above with your actual credentials
# 4. The bucket name should match your R2 bucket name (currently set to 's3-uploader') 